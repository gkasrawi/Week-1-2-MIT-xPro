setInterval()
// The setInterval method calls a function or evaluates an expression after a specified number of milliseconds.
// The setInterval method calls will continue calling the function until clearInterval() is called or the window is closed. 

setInterval(callAgain, 3000)  // SetInterval will continue to lapse after 3 seconds. s